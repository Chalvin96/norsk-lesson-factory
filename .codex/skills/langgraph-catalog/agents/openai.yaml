interface:
  display_name: "LangGraph Catalog"
  short_description: "Run and inspect the catalog LangGraph safely"
  default_prompt: "Use $langgraph-catalog to inspect or run a catalog-design LangGraph proposal."
