"""Entry points: import responsibility-named lesson-domain service modules.

The package has no single runtime entry point. Import a responsibility-named
service module directly: ``exercise_diagnostics`` records deterministic
exercise evidence, and ``transcript`` projects Norwegian audio sources. Review
payload projections belong to the lesson validation namespace.
"""
